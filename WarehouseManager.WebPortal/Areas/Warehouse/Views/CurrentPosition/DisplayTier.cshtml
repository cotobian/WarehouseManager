@model WarehouseManager.ViewModels.Warehouse.CurrentPosition.DisplayTierVm
@{
    Layout = null;
}
<!DOCTYPE html>
<html>
<head>
<style>
    .container-tier {
        display: flex;
        flex-direction: column;
        justify-content: flex-start;
        align-items: center;
        height: 500px;
    }
    
    .tier {
        width: 100px;
        height: 100px;
        color: black;
        border: 1px solid blue;
        margin-top: 10px;
        border-radius: 0.35rem;
    }
</style>
</head>
<body>

<div class="container-tier">
    <span class="tier-text">01</span>
    <div class="tier">
        @foreach (var item in Model.ListItem[0])
        {
           @item.ToString()
        }
    </div>
    <span class="tier-text">02</span>
    <div class="tier">
        @foreach (var item in Model.ListItem[1])
        {
           @item.ToString()
        }
    </div>
    <span class="tier-text">03</span>
    <div class="tier">
        @foreach (var item in Model.ListItem[2])
        {
           @item.ToString()
        }
    </div>
    <span class="tier-text">04</span>
    <div class="tier">
        @foreach (var item in Model.ListItem[3])
        {
           @item.ToString()
        }
    </div>
    <span class="tier-text">05</span>
    <div class="tier">
        @foreach (var item in Model.ListItem[4])
        {
           @item.ToString()
        }
    </div>
    <span class="tier-text">06</span>
    <div class="tier">
        @foreach (var item in Model.ListItem[5])
        {
           @item.ToString()
        }
    </div>
</div>

</body>
</html>